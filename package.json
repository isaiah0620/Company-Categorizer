{
  "name": "company-categorizer-ts",
  "version": "1.0.0",
  "description": "Categorizes companies by polling Postgres (metadata->>'checked' is not true), scraping with Firecrawl or Tavily, and classifying with either Claude or OpenAI (LLM_PROVIDER) using prompt caching. Token usage is written back into the row's JSONB.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "run-once": "node dist/index.js --once",
    "dev": "tsx src/index.ts",
    "dev:once": "tsx src/index.ts --once",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "node-cron": "^3.0.3",
    "openai": "^7.22.0",
    "pg": "^8.13.0",
    "tunnel-ssh": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
